---
description: Code style, [language], and framework-specific patterns
globs:
  - "**/*.[extensions]"
---

# Coding Patterns

> **TEMPLATE**: Copy this file to `/.cursor/rules/coding-patterns.mdc`.
> Replace all `[bracketed]` placeholders with your project's values.
> See `../../examples/cursor-rules/coding-patterns.mdc` for a filled-in example.

---

## [Language] Configuration

### Key Settings

```json
{
  "[setting1]": [value],
  "[setting2]": [value]
}
```

### Rules

- [Rule 1]
- [Rule 2]
- [Rule 3]
- [Rule 4]

---

## Naming Conventions

### Files and Directories

| Type | Convention | Example |
|------|------------|---------|
| Components | [Convention] | `[Example].tsx` |
| Hooks | [Convention] | `[Example].ts` |
| Utils | [Convention] | `[Example].ts` |
| Types | [Convention] | `[Example].ts` |
| Constants | [Convention] | `[Example].ts` |
| Directories | [Convention] | `[example]/` |

### Code

```[language]
// Variables - [convention]
[example]

// Constants - [convention]
[example]

// Functions - [convention]
[example]

// Types/Interfaces - [convention]
[example]

// Components - [convention]
[example]

// Hooks - [convention]
[example]
```

---

## Component Patterns

### [Component Type] Components

```[language]
// [Pattern description]
[example code]
```

### Component Structure

```[language]
export function [Component]({ [props] }: [Props]) {
  // 1. Hooks (always at the top)
  
  // 2. Derived/computed values
  
  // 3. Event handlers
  
  // 4. Effects (if needed)
  
  // 5. Early returns
  
  // 6. Render
  return (/* JSX */)
}
```

---

## Type Patterns

### [Type Pattern 1]

```[language]
[example]
```

### [Type Pattern 2]

```[language]
[example]
```

---

## Error Handling

```[language]
[error handling pattern example]
```

---

## Import/Export Patterns

### [Pattern 1]

```[language]
[example]
```

### [Pattern 2]

```[language]
[example]
```

---

## Anti-Patterns to Avoid

```[language]
// WRONG - [description]
[bad example]

// CORRECT - [description]
[good example]
```

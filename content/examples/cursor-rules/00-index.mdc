---
description: Master index of all Cursor rules - AI agents should load this first
globs:
  - "**/*"
---

# SpaceBooker - Cursor Rules Index

> **REFERENCE ONLY**: This is a filled-in example showing the expected format.
> To create your own file, use `../../templates/cursor-rules/00-index.mdc` and replace the placeholders.
> Do NOT copy this content directly - adapt the STRUCTURE, not the CONTENT.

---

## Rule Categories

### Core Rules (Always Relevant)

| Rule | File | When to Load |
|------|------|--------------|
| **Project Architecture** | `project-architecture.mdc` | Always - project structure |
| **Coding Patterns** | `coding-patterns.mdc` | Always - code conventions |

### Task-Specific Rules

| Task Type | Rule File | Description |
|-----------|-----------|-------------|
| **State/Data** | `state-management.mdc` | Data fetching, caching, state |
| **Testing** | `testing-strategy.mdc` | Test approach and patterns |
| **Documentation** | `documentation.mdc` | Doc maintenance guidelines |

---

## Loading Priority by Task

### Building a New Feature
1. `project-architecture.mdc` - Where files go
2. `coding-patterns.mdc` - How to write code
3. `state-management.mdc` - Data handling
4. `documentation.mdc` - What to document

### Writing Tests
1. `testing-strategy.mdc` - Test philosophy and patterns

### Fixing Style Issues
1. `coding-patterns.mdc` - Conventions and fixes

### Updating Documentation
1. `documentation.mdc` - Guidelines

---

## Quick Reference

| # | File | Purpose |
|---|------|---------|
| 1 | `project-architecture.mdc` | Project structure, routing |
| 2 | `coding-patterns.mdc` | Code style, conventions |
| 3 | `state-management.mdc` | Data fetching, state |
| 4 | `testing-strategy.mdc` | Test strategy |
| 5 | `documentation.mdc` | Doc guidelines |

---

## Critical Reminders

### Path Aliases

```tsx
// WRONG
import { Button } from '../../../components/Button'

// CORRECT
import { Button } from '@/components/Button'
```

### Server vs Client Components

```tsx
// Default: Server Component (no directive needed)
export default async function SpacesPage() {
  const spaces = await getSpaces()
  return <SpaceList spaces={spaces} />
}

// Client Component: Only when needed
'use client'
export function BookingForm() {
  const [date, setDate] = useState()
  return <form>...</form>
}
```

### Booking Validation

```tsx
// ALWAYS check availability before creating booking
const isAvailable = await checkSpaceAvailability(spaceId, date)
if (!isAvailable) {
  throw new Error('Space not available for selected date')
}
```

---

## Documentation Locations

| Audience | Location | Purpose |
|----------|----------|---------|
| AI Agents | `.cursor/rules/*.mdc` | Detailed patterns |
| AI Agents | `AGENTS.md` | Quick context |
| Developers | `docs/INDEX.md` | Navigation |
| Both | `docs/TASKS.md` | Task tracking |
